import { NextResponse } from 'next/server'; import { getSessionUser } from '@/lib/auth'; import { database } from '@/lib/db'; import { apiError } from '@/lib/http';
export async function GET(){const user=await getSessionUser();if(!user)return apiError('Authentication required.',401);const stages=await database`select stage,count(*)::int count from applications where user_id=${user.id} group by stage`;const scores=await database`select r.filename,a.score,a.created_at from resume_analyses a join resumes r on r.id=a.resume_id where r.user_id=${user.id} order by a.created_at desc limit 20`;const [totals]=await database`select (select count(*)::int from projects where user_id=${user.id}) projects,(select count(*)::int from certificates where user_id=${user.id}) certificates,(select count(*)::int from cover_letters c join resumes r on r.id=c.resume_id where r.user_id=${user.id}) cover_letters`;const [funnel]=await database`select count(*)::int applications,count(*) filter(where stage in ('Screening','Assessment','Interview','Final interview','Offer'))::int progressed,count(*) filter(where stage='Offer')::int offers,count(*) filter(where follow_up_at is not null and follow_up_at<=current_date and stage not in ('Offer','Rejected','Withdrawn'))::int follow_ups_due from applications where user_id=${user.id}`;const activity=await database`select to_char(date_trunc('month', applied_at),'Mon YYYY') month,count(*)::int count from applications where user_id=${user.id} and applied_at >= (current_date - interval '5 months') group by 1,date_trunc('month',applied_at) order by date_trunc('month',applied_at)`;return NextResponse.json({stages,scores,totals,funnel:{...funnel,interview_rate:funnel.applications?Math.round(funnel.progressed/funnel.applications*100):0,offer_rate:funnel.applications?Math.round(funnel.offers/funnel.applications*100):0},activity})}
